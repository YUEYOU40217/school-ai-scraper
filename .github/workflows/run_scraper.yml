name: Run Scraper

on:
  schedule:
    # 僅在 UTC 的 2點、10點、18點的第 37 分鐘觸發（對應台灣時間 10:37, 18:37, 02:37）
    # 一天只發車 3 次，徹底避開每小時執行的懲罰機制
    - cron: '37 2,10,18 * * *'
  workflow_dispatch:

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: '**/requirements.txt'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run python script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python scraper.py

      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add announcements.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto update: incremental sync and UUID lock" && git push)
