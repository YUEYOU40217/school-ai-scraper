name: Run Scraper

on:
  schedule:
    - cron: '18 2,6,10,14,18,22 * * *'
  workflow_dispatch:

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: '**/requirements.txt'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Fetch old data from gh-pages
        run: |
          git fetch origin gh-pages || true
          git show origin/gh-pages:announcements.json > announcements.json || true
          git show origin/gh-pages:raw_scraped.json > raw_scraped.json || true

      - name: Run python script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SCRAPER_API_KEY: ${{ secrets.SCRAPER_API_KEY }}
        run: python scraper.py

      - name: Commit and Push to gh-pages
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # 將最終產出與原始抓取紀錄備份至暫存區
          cp announcements.json /tmp/announcements.json
          cp raw_scraped.json /tmp/raw_scraped.json || true
          
          git checkout -- announcements.json || true
          git checkout -- raw_scraped.json || true
          
          git fetch origin gh-pages
          git checkout gh-pages
          
          # 從暫存區復原並備份至 gh-pages 分支
          cp /tmp/announcements.json announcements.json
          cp /tmp/raw_scraped.json raw_scraped.json || true
          
          git add announcements.json raw_scraped.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto update: sync data and raw logs to gh-pages" && git push origin gh-pages)
