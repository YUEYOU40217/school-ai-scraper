name: Run Scraper

on:
  schedule:
    - cron: '18 2,6,10,14,18,22 * * *'
  workflow_dispatch:

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: '**/requirements.txt'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Fetch old data from gh-pages
        run: |
          git fetch origin gh-pages || true
          git show origin/gh-pages:announcements.json > announcements.json || true

      - name: Run python script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SCRAPER_API_KEY: ${{ secrets.SCRAPER_API_KEY }}
        run: python scraper.py

      - name: Commit and Push to gh-pages
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # 將兩個 JSON 都先複製到暫存區
          cp announcements.json /tmp/announcements.json
          cp raw_crawled_data.json /tmp/raw_crawled_data.json || true
          
          # 放棄目前分支的變更，準備切換
          git checkout -- announcements.json raw_crawled_data.json || true
          
          git fetch origin gh-pages
          git checkout gh-pages
          
          # 從暫存區放回 gh-pages 分支
          cp /tmp/announcements.json announcements.json
          cp /tmp/raw_crawled_data.json raw_crawled_data.json || true
          
          # 同時追蹤並提交這兩個檔案
          git add announcements.json raw_crawled_data.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto update: sync data and raw logs to gh-pages" && git push origin gh-pages)
